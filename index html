<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Bento Grid Landing Page - Syarifah Irdina Nazmie</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    <style>
        /* CSS Reset and Global Styles */
        :root {
            /* 4. Clean, scientific, and modern color palette */
            --color-background: #f4f6f8; /* Soft Grey Background */
            --color-card-bg: #ffffff; /* White Card Background */
            --color-text: #1f2937; /* Dark Text */
            --color-subtle-text: #6b7280; /* Subdued Text */
            /* Theme Update: Deep 'Ocean Blue' for UMT theme */
            --color-accent: #023e8a; 
            --color-accent-light: #0077b6; 
            --color-border: #e5e7eb; /* Light Border */
            --shadow-subtle: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -2px rgba(0, 0, 0, 0.05);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--color-background);
            color: var(--color-text);
            min-height: 100vh;
            padding: 24px;
        }

        a {
            color: var(--color-accent);
            text-decoration: none;
            font-weight: 500;
        }

        /* Bento Grid Container */
        .bento-grid {
            /* 2. Grid layout on desktop (3 columns base) */
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            max-width: 1200px; /* Max width for central alignment */
            margin: 0 auto;
        }

        /* General Card Styles */
        .bento-card {
            background-color: var(--color-card-bg);
            border-radius: 12px;
            padding: 24px;
            border: 1px solid var(--color-border);
            box-shadow: var(--shadow-subtle);
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        
        /* Subtle hover effect for polish */
        .bento-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1);
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 8px;
            color: var(--color-text);
        }

        .card-content {
            color: var(--color-subtle-text);
            font-size: 0.95rem;
            line-height: 1.5;
        }

        /* 1. Grid Span Layouts (Desktop) */
        .card-profile { grid-column: span 2; grid-row: span 2; }
        .card-playlist { grid-column: span 3; }
        .card-location, .card-hobby { grid-column: span 1; }
        .card-tech-stack, .card-case-study { grid-column: span 1; }

        /* Specific Card Styling */
        .social-links {
            display: flex;
            gap: 16px;
            margin-top: 16px;
        }

        .social-links a {
            color: var(--color-accent);
            transition: color 0.2s ease;
        }

        .social-links a:hover {
            color: var(--color-accent-light);
        }
        
        /* Mobile Responsiveness (2. Stacked vertically on mobile) */
        @media (max-width: 768px) {
            body { padding: 16px; }

            .bento-grid {
                grid-template-columns: 1fr;
                gap: 16px;
            }

            .bento-card {
                grid-column: span 1 !important;
                grid-row: span 1 !important;
            }
        }
    </style>
</head>
<body>
    <script>
        // JavaScript to update the time dynamically (client-side)
        function updateTime() {
            const now = new Date();
            const timeString = now.toLocaleTimeString('en-MY', {
                hour: '2-digit',
                minute: '2-digit',
                hour12: true,
                timeZone: 'Asia/Kuala_Lumpur' // Kuala Terengganu/Kuala Nerus time zone
            });
            document.getElementById('current-time').textContent = timeString;
        }
        // Run immediately and then every minute
        document.addEventListener('DOMContentLoaded', () => {
            updateTime();
            // Update time every 60 seconds
            setInterval(updateTime, 60000); 
        });
    </script>

    <div class="bento-grid">
        

        <div class="bento-card card-profile">
            <div>
                <h1 class="card-title" style="font-size: 2.5rem; color: var(--color-accent);">Syarifah Irdina Nazmie Binti Wan Nazarudin</h1>
                <p class="card-content" style="font-size: 1.1rem; margin-bottom: 8px;">
                    **Food Science** Student
                </p>
                <p class="card-content">
                    **Year:** Year 1 
                </p>
                <p class="card-content" style="margin-top: 16px;">
                    **Tagline:** *Innovating food safety.* Dedicated to understanding and improving the science behind our food supply.
                </p>
            </div>
            <div class="social-links">
                <a href="#" aria-label="LinkedIn Profile">LinkedIn</a>
                <a href="#" aria-label="GitHub Profile">GitHub</a>
                <a href="#" aria-label="UMT Student Portal">UMT Portal</a>
            </div>
        </div>

        <div class="bento-card card-location">
            <h2 class="card-title">Current Location</h2>
            <p class="card-content">
                **Campus:** UMT, Kuala Nerus
            </p>
            <p class="card-content">
                **Local Time:** <span id="current-time">Loading...</span> 
            </p>
        </div>

        <div class="bento-card card-tech-stack">
            <h2 class="card-title">Core Subjects & Skills</h2>
            <ul class="card-content" style="list-style-type: disc; margin-left: 20px;">
                <li>Food Chemistry</li>
                <li>Microbiology & Safety</li>
                <li>Data Analysis (Excel)</li>
                <li>Instrumental Lab Analysis</li>
            </ul>
        </div>

        <div class="bento-card card-case-study">
            <h2 class="card-title">Featured Mini Case Study</h2>
            <p class="card-content">
                **Title:** Starch Gelatinization in Local Kuih
            </p>
            <p class="card-content" style="margin-top: 10px;">
                *Analyzing the texture and thermal profiles of **Kuih Lapis** to optimize ingredient ratios and processing methods.* 
            </p>
        </div>

        <div class="bento-card card-hobby">
            <h2 class="card-title">Recreation</h2>
            <p class="card-content">
                **Activity:** Evening walks
            </p>
            <p class="card-content">
                *Recharge and clear the mind with walks along **Pantai UMT**.*
            </p>
        </div>
        
        <div class="bento-card card-playlist">
            <h2 class="card-title">Study Focus Playlist</h2>
            <p class="card-content">
                *Instrumental Hip-Hop and Ambient Music for deep work sessions.*
            </p>
            <div style="height: 60px; background-color: var(--color-border); border-radius: 6px; margin-top: 15px; display: flex; align-items: center; justify-content: center;">
                <p style="color: var(--color-subtle-text);">Music Player Widget Placeholder (Spotify/YouTube Music)</p>
            </div>
        </div>

    </div>

</body>
</html>